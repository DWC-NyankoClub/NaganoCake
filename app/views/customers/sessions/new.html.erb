<div class="container row mx-auto">
  <div class="signin text-center py-5 px-3 col-md-6 mx-auto rounded">
    <h4 class="signin-title mb-5">ログイン</h4>
    <h5 class="mb-3">会員の方はこちらからログイン</h5>
    <%= form_with model: @customer, url: customer_session_path, class: "col-12", local: true do |f| %>

      <div class="field mb-3">
        <i class="far fa-envelope mr-2"></i><%= f.label "メールアドレス" %><br />
        <%= f.email_field :email, autofocus: true, autocomplete: "email", size: "40" %>
      </div>
      <div class="field mb-3">
        <i class="fas fa-key mr-2"></i><%= f.label "パスワード" %><br />
        <%= f.password_field :password, autocomplete: "current-password", size: "40" %>
      </div>
      <%= f.submit "ログイン", class: "btn btn-sm btn-primary col-lg-2 px-0", size: "40"%>
    <% end %>
    <div class="mt-5">
      <h4>会員登録がお済みでない方</h4>
      <%= link_to "こちら", new_customer_registration_path %>　から会員登録を行ってください。
    </div>
  </div>
</div>